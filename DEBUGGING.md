# Debugging Log

Real problems hit while building, in the order we hit them. Each entry:
symptom → diagnosis → root cause → fix → verification.

> No issues logged yet — this file grows as we build. Do not add invented
> problems here.
